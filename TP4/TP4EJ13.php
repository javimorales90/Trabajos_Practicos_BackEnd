<?php

/*

Ingresar repetidamente el código (DNI) del estudiante (tipo entero) y dos notas Nota1 y Nota2 de los exámenes
(tipo real) la finalización del ingreso de datos es a cuando se ingresa un el código igual a 999 (validar que
las notas sean valores mayores o iguales a cero). Calcular el promedio de las notas, mediante un módulo denominado
promedioNotas. Asignar la calificación de acuerdo a las notas de la tabla, hacer el algoritmo, usando un módulo
denominado calificacionNotas que recibe un parámetro formal X de tipo real, que es la Nota promedio del estudiante
y que devuelve una cadena de caracteres con la calificación correspondiente, como se nota en la tabla siguiente:

    VER CUADRO EN PDF DE TP

*/
